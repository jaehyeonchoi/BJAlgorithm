#include <iostream>
#include <string>

using namespace std;

int main() {
	int a, b;

	cin >> a >> b;

	if (b >= 45) {
		b = b - 45;
	}
	else {
		if (a > 0) {
			a = a - 1;
		}
		else {
			a = 23;
		}
		b = b + 15;
	}
	cout << a << " " << b;

	return 0;
}
